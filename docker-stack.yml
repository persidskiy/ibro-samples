version: "3"
services:
  web:
    image: cr.yandex/crporpe9rktdmdidsp0v/ibro-samples_web
    build: web
    container_name: web
    hostname: web
    restart: always
    environment:
      - MODE=PRODUCTION

  nginx:
    image: cr.yandex/crporpe9rktdmdidsp0v/ibro-samples_nginx
    build: nginx
    hostname: nginx
    restart: always
    container_name: nginx
    depends_on:
      - web

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

    volumes:
      - /opt/ibro-samples/ssl:/etc/nginx/ssl
    ports:
      - "80:80"
      - "443:443"

    environment:
      - TZ=Europe/Moscow
      - LETSENCRYPT=true
      - LE_EMAIL=${LE_EMAIL}
      - LE_FQDN=${LE_FQDN}
